# Copy to `wifi-autoconnect.env` and fill in real values.
# This file is read by wifi-autoconnect.service via EnvironmentFile=.
# Deploy the populated file to /etc/default/wifi-autoconnect on the board.

WIFI_SSID=your-ssid-here
WIFI_PASS=your-password-here
WIFI_IFACE=wlan0
